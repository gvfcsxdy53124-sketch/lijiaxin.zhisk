# Permissions Feature

- `pages`: route-level pages for permission management.
- `components`: components used only by the permissions domain.
- `hooks`: permissions-specific React hooks.
- `store`: permissions-specific Zustand stores.
- `api`: permissions-specific API clients.
